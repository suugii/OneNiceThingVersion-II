import { FitContentsDirective } from './fit-contents.directive';

describe('FitContentsDirective', () => {
  it('should create an instance', () => {
    const directive = new FitContentsDirective();
    expect(directive).toBeTruthy();
  });
});
